#include <stdio.h>
#include <stdlib.h>

void shellSort(int data[], int n)
{
    int *delta, k, i, t, dk, j;
    k = n;
    delta = (int *)malloc(sizeof(int) * (n / 2));
    // Your code here
    free(delta);
}